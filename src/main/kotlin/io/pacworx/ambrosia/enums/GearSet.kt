package io.pacworx.ambrosia.io.pacworx.ambrosia.enums

/**
 * To add a new set you need to:
 * - add the set name to this enum
 * - add dynamic properties with the sets name and the bonuses
 * - add the set name to JewelType
 * - add dynamic properties for the special jewel
 */
enum class GearSet {
    STONE_SKIN,
    VITAL_AURA,
    POWER_FIST
}
